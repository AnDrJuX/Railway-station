<p id="notice"><%= notice %></p>

<p>
  <strong>Номер поезда:</strong>
  <%= @train.number %>
</p>

<p>
  <strong>На станции:</strong>
  <% if @train.current_station.present? %>
      <%= @train.current_station.title %>
  <% else %>
      Не указано
  <% end %>
</p>

<p>
  <strong>На маршруте:</strong>
  <% if @train.route.present? %>
      <%= @train.route.title %>
  <% else %>
      Маршрут не назначен
  <% end %>
</p>

<p>
  <strong>Вагонов у поезда:</strong>
  <% if @train.wagons.present? %>
      <%= @train.wagons.count %><br>
      Плацкартных:
      <%= @train.wagon_type('Плацкарт') %> |
      Нижних мест:
      <%= @train.wagon_bottom_seats('Плацкарт', :bottom_place) %> |
      Верхних мест:
      <%= @train.wagon_top_seats('Плацкарт', :top_place) %><br>
      Купе:
      <%= @train.wagon_type('Купе') %> |
      Нижних мест:
      <%= @train.wagon_bottom_seats('Купе', :bottom_place) %> |
      Верхних мест:
      <%= @train.wagon_top_seats('Купе', :top_place) %>
  <% else %>
      0
  <% end %>
</p>


<%= link_to 'Редактировать', edit_train_path(@train) %> |
<%= link_to 'Назад', trains_path %>